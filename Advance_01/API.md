# What is API?

### API (Application programmming Interface)

API can be undersatnd as a means to establice a communication standard between differnt application wherather it be between two different application runing running on differnt lang (one being in java other python), or just for the frotend or backend of a application. In big projects or Application there are mutiple feature for which differnt stack can be used . API provide a standization and protol.

``` mermaid
flowchart TD

```
* `Web APIs:` What we usually mean today (REST, GraphQL).

* `Library/Library APIs:` When you import a library in Java or Python, the functions it gives you are its API.

* `OS APIs:`  How software talks to your computer's hardware (like the Windows API).

> This is the ***instruction manual*** for developers. Without it, other developers won't know which endpoints to call or what data format to send 